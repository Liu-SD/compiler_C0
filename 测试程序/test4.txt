int mod(int x, int y) {
	return (x - x / y * y);
}

int gcd(int x, int y) {
	if(y == 0) return (0);
	else if( mod(x, y) == 0) return (y);
	else return (gcd(y, mod(x, y)));
}

void main() {
	int x, y;
	char c;
	scanf(x, y);
	printf("gcd: ", gcd(x, y));
	scanf(c);
	printf(c);
}